<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leaf Cup</title>

    <!-- Tabulator CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tabulator-tables@5.0.7/dist/css/tabulator.min.css" rel="stylesheet">

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        @import url(https://fonts.googleapis.com/css2?family=Droid+Sans:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap);

        /* General Tabulator Styles */
        .tabulator {
            width: 100%;
            margin-top: 1.25rem;
            border-radius: 0.375rem;
            overflow: hidden;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
        
        .tabulator-tableholder {
            background-color: #ffffff;
        }

        div.tabulator-col.tabulator-sortable {
            background-color: #c9daf8!important;
            font-size: 12px!important;
            font-weight: 500;
            text-align: center!important;
            color: #000000;
            font-family: Droid Sans;
            }
        
        /* Header Styles */
        .tabulator-header {
            background-color: #edf2f7;
        }
        
        .tabulator-col {
            padding: 0.75rem;
            border-bottom: 1px solid #d2d6dc;
        }
        
        .tabulator-col:hover {
            background-color: #f7fafc;
        }
        
        .tabulator-arrow {
            display: inline-block;
            width: 1rem;
            height: 1rem;
            color: #718096;
        }
        
        .tabulator-header-filter {
            margin-top: 0.5rem;
        }
        
        /* Row Styles */
        .tabulator-row {
            border-bottom: 1px solid #edf2f7;
        }
        
        .tabulator-row:hover {
            background-color: #f7fafc;
        }
        
        .tabulator-cell {
            padding: 0.75rem;
            text-align: center;
            font-family: Droid Sans;

        }
        
        .tabulator-selected {
            background-color: #e2e8f0;
        }
        
        /* Other styles can be added in the same manner */
        </style>
            
    
</head>

<body class="bg-gray-100 font-sans">

<h2 class="text-2xl font-bold m-5 text-center">Leaf Cup</h2>

<div class="container mx-auto p-5 bg-white rounded shadow-md">
    <!-- Table placeholder for Tabulator -->
    <div id="leaderboardTable"></div>
</div>

<!-- Tabulator JS and Initialization -->
<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/tabulator-tables@5.0.7/dist/js/tabulator.min.js"></script>
<script>
    var leaderboardData = <%- JSON.stringify(leaderboard) %>;

    var table = new Tabulator("#leaderboardTable", {
    data: leaderboardData,
    responsiveLayout:"hide",
    layout:"fitColumns",
    columns: [
        {title: "Name", field: "name", sorter:"string"},
        {title: "Score", field: "score", sorter:"number"},
        {title: "Car Number", field: "carNumber", sorter:"string"},
        {title: "Penalties", field: "penalties", sorter:"number"},
    ]
});


// Add custom table class after initialization
document.getElementById("leaderboardTable").classList.add("custom-table");
</script>

</body>
</html>
